# Copy to `.env` (do not commit) and fill in to send OTP emails via real SMTP.
#
# For local development, you can leave these unset to use Mailpit:
# - Mailpit UI: http://localhost:8025
#
# Database (optional - if you want to configure DB via env vars)
# Replace these with your actual DB credentials (do not commit real secrets)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=eteeap_survey
DB_USER=your_db_user
DB_PASS=your_db_password

# SMTP_ENCRYPTION:
# - none      : plain SMTP (Mailpit default)
# - starttls  : SMTP with STARTTLS (commonly port 587)
# - ssl       : SMTPS / implicit TLS (commonly port 465)

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_ENCRYPTION=starttls
SMTP_AUTH=true
SMTP_USER=your@email.com
SMTP_PASS=your_smtp_password_or_app_password
SMTP_FROM_EMAIL=your@email.com
SMTP_FROM_NAME=ETEEAP Survey OTP
SMTP_FORCE_IPV4=false
SMTP_TIMEOUT=15
# Optional: comma/space separated list of hosts/IPs to try in order (advanced)
# SMTP_CONNECT_HOSTS=smtp.example.com,1.2.3.4
# Optional: safe debug logging to storage/logs/otp.log (no secrets)
# SMTP_DEBUG_LOG=false

# OTP HMAC secret (required for OTP generation/verification)
OTP_SECRET=replace-with-a-long-random-secret
